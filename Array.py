# Creating an array (list)
numbers = [10, 20, 30, 40]
name=["pooja","rani","kajal"]

# Accessing elements
print(numbers[0])      # 10

# Modifying elements th
numbers[1] = 25

# Adding elements for add
numbers.append(50)

# Removing elements
numbers.remove(30)

# Loop through array
for num in numbers:
    print(num)

# Length of array
print("Length:", len(numbers))
print(name)
